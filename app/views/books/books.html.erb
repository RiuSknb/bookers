<h1>Books</h1>

<p>title</p>
<p>body</p>

<% @books.each do |book| %>
  <span>
    <%= book.title %>
    <%= book.body %>
    <%= link_to 'Show', book_path(book.id) %>

    <!--link_toメソッドではデフォルトでリンク先をshowアクションに設定しています。
    Editをクリックした際には、editアクションにリンクする必要があります。

    そのため、`link_to`メソッドの第二引数に`edit_book_path`ヘルパーメソッドを
    使用するように変更します。-->

    <%= link_to 'Edit', edit_book_path(book.id) %>
    <%= link_to 'Destroy', book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %>

  </span>

<% end %>
<!--<p>edit</p>-->
<!--<p>destroy</p>-->







<!-- form_with部分 -->

<h1>New book</h1>
<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<%= form_with model: @book, url:'/books', method: :post do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>
<% end %>